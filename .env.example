# MongoDB (required for data — appointments, users, etc.)
MONGO_URI=mongodb://127.0.0.1:27017/hospital_db

# Server
PORT=3000

# Sessions: use 1 for reliable local login without Mongo session store
USE_MEMORY_SESSION=1

SESSION_SECRET=change_this_secret_key

# Optional
NODE_ENV=development
